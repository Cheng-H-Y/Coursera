# template for "Ship"# run on http://www.codeskulptor.org/class ship    def __init__(self,pos,vel,angle,image,info):        self.pos=pos[]        self.vel=vel[]        self.angle=angle        self.thrust=False        self.angle_vel=0        self.image=image    def draw(self,canvas):        canvas.